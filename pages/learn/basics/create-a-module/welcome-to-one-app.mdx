import Layout from '../../../../components/learn/Layout'
import Image from '../../../../components/image'

export const meta = {
  title: 'Create a One App Module',
  courseId: 'basics',
  lessonId: 'create-a-module',
  stepId: 'welcome-to-one-app',
  subtitle: 'Welcome to One App'
}

You should see a page like this when you access [http://localhost:3000](http://localhost:3000).

<Image
  shadow
  alt="Welcome to One App"
  oversize={false}
  src="/static/images/learn/create-a-module/welcome-to-one-app.png"
  width={600 / 2}
  height={170 / 2}
/>

Let’s try to edit this page next!

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
