import Layout from '../../../../components/learn/Layout'
import SkipPageButton from '../../../../components/learn/SkipPageButton.js'

export const meta = {
  title: 'Routing',
  courseId: 'basics',
  lessonId: 'routing',
  stepId: 'setup'
}

**If you’re continuing from the previous lesson,** you can skip this page. Click the button below to go to the next page.

<SkipPageButton meta={meta} />

### Download Starter Code (Optional)

If you’re NOT continuing from the previous lesson, follow the instructions [for getting setup](/learn/basics/create-a-module/setup).

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
